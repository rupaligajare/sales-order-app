# Copy to .env for local overrides (optional). On Render, set this in the Static Site dashboard.
# No trailing slash. Example after backend is deployed:
# VITE_API_BASE_URL=https://portal-api.onrender.com
VITE_API_BASE_URL=http://localhost:8080
